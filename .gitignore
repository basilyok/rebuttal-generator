# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local wrangler dev state (miniflare SQLite, bundle temp files) — regenerated
# per-machine by `wrangler dev`, never something to commit.
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.local
.env.*.local
.dev.vars

# Git worktrees for isolated task execution
.worktrees

# Machine-local Claude Code settings
.claude/settings.local.json
